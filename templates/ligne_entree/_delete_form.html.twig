<form method="post" action="{{ path('ligne_entree_delete', {'id': ligne_entree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_entree.id) }}">
    <button class="btn">Delete</button>
</form>
