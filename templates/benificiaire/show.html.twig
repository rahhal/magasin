{% extends 'base.html.twig' %}

{% block title %}Benificiaire{% endblock %}

{% block body %}
    <h1>Benificiaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ benificiaire.id }}</td>
            </tr>
            <tr>
                <th>Benificiaire</th>
                <td>{{ benificiaire.benificiaire }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ benificiaire.remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('benificiaire_index') }}">back to list</a>

    <a href="{{ path('benificiaire_edit', {'id': benificiaire.id}) }}">edit</a>

    {{ include('benificiaire/_delete_form.html.twig') }}
{% endblock %}
