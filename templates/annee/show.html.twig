{% extends 'base.html.twig' %}

{% block title %}Annee{% endblock %}

{% block body %}
    <h1>Annee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annee.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ annee.libelle }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ annee.designation ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('annee_index') }}">back to list</a>

    <a href="{{ path('annee_edit', {'id': annee.id}) }}">edit</a>

    {{ include('annee/_delete_form.html.twig') }}
{% endblock %}
