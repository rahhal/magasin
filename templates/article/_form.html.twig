{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="text-right">
    <button class="btn btn-primary"><i class="glyphicon glyphicon-save" style="margin-left: 1rem"></i>{{ button_label|default('Save')|trans }}</button>
</div>
    {{ form_end(form) }}